/// <reference path="typings/angular2/angular2.d.ts" />

/*
  This is the main file that loads the root component
  and loads it.
*/
import {bootstrap} from 'angular2/angular2';
import {Root} from 'components/root/root';

bootstrap(Root);
